# No secrets are required for this coursework pipeline.
# Optional: override the config path used by `python main.py`.
# Use an absolute path or a path relative to the project root.
MLF_C2O_CONFIG=config/default.yaml
